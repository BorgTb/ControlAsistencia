# ============================================
# VARIABLES DE ENTORNO - BACKEND
# ============================================
# IMPORTANTE: Este es un archivo de ejemplo
# Copia este archivo como .env y completa con tus valores reales
# NUNCA commitees el archivo .env real con datos sensibles
# ============================================

# ============================================
# CONFIGURACIÓN DEL SERVIDOR
# ============================================
SERVER_PORT=3000
NODE_ENV=development

# ============================================
# CONFIGURACIÓN DE SEGURIDAD
# ============================================
# Clave secreta para firmar JWT - CAMBIAR EN PRODUCCIÓN
# IMPORTANTE: Usa una clave larga y aleatoria (mínimo 32 caracteres)
# Genera una con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SECRET_KEY=tu_clave_secreta_super_segura_cambiar_en_produccion_minimo_32_caracteres

# URL del frontend para CORS
FRONTEND_URL=http://localhost:5173

# ============================================
# CONFIGURACIÓN DE TOKENS (OPCIONAL)
# ============================================
# Tiempo de expiración del Access Token (por defecto: 15 minutos)
# ACCESS_TOKEN_EXPIRY=15m

# Tiempo de expiración del Refresh Token (por defecto: 30 días)
# REFRESH_TOKEN_EXPIRY=30d

# ============================================
# BASE DE DATOS
# ============================================
DB_HOST=localhost
DB_USER=tu_usuario_db
DB_PASSWORD=tu_password_db
DB_NAME=control_asistencia
DB_PORT=5432

# ============================================
# CONFIGURACIÓN DE EMAILS (si aplica)
# ============================================
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=tu_email@gmail.com
MAIL_PASSWORD=tu_password_email
MAIL_FROM=noreply@tudominio.com

# ============================================
# OTRAS CONFIGURACIONES
# ============================================
# Tiempo de expiración del token JWT (en formato válido para jsonwebtoken)
JWT_EXPIRATION=1h

# Nivel de logs
LOG_LEVEL=debug
